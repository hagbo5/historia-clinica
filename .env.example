# This is an example file. Copy this to a new file named .env
# and replace the placeholder values with your actual credentials.
# The .env file should NOT be committed to version control.

ICD_API_CLIENT_ID="YOUR_ICD_API_CLIENT_ID_HERE"
ICD_API_CLIENT_SECRET="YOUR_ICD_API_CLIENT_SECRET_HERE"

# Optional Flask environment variables (uncomment and set if needed):
# FLASK_APP="index.py"
# FLASK_ENV="development"
# SECRET_KEY="your_flask_secret_key_here" # If you want to override the one in index.py for development
